# Discord Cloud Storage

Cloud storage pribadi menggunakan Discord sebagai backend.

## Komponen:
1. Bot (di folder `bot/`)
2. Backend API (di folder `backend/`)
3. Frontend Web (di folder `frontend/`)
